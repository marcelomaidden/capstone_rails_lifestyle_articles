<!DOCTYPE html>
<html>
  <head>
    <title>LifestyleArticle</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class = 'container-fluid p-4 bg-light sticky-top'>
      <nav class = 'd-flex'>
        <div class = 'col-3'>
          Logo
        </div>
        <div class = 'col-6 font-weight-bold orange-link'>
          <%= link_to  "HOME", root_path %>
        </div>
        <div class = 'd-flex col-3 justify-content-end'>
          <% if session[:current_user].nil? %>
            <div class = 'col font-weight-bold'><%= link_to  "LOG-IN", login_path %></div>
          <% else %>
            <div class = 'col font-weight-bold'>
              <% if session[:current_user] %>
                <%= link_to session[:current_user]['name'], user_path(session[:current_user]['id']) %>   
              <% end %>
            </div>
            <div class = 'col font-weight-bold'>
              <%= link_to  "LOG-OUT", logout_path %>
            </div>
          <% end %>
          <div class = 'col font-weight-bold'><%= link_to  "REGISTER", new_user_path %></div>
        </div>
      </nav>
    </header>
    <% flash.each do |type, msg| %>
      <div class = 'p-3 mb-2 bg-dark text-white d-flex justify-content-center'>
        <%= msg %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
